[build]
  publish = "."
  functions = "netlify/functions"

[functions]
  node_bundler = "esbuild"

# Redireciona /api/* para as functions sem expor a URL interna
[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200